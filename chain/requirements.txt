# Production dependencies (used by the API server)
fastapi>=0.139.0
uvicorn>=0.51.0
supabase>=2.31.0
python-dotenv>=1.2.2
slowapi>=0.1.10
pydantic>=2.13.4
PyNaCl>=1.6.2
numpy>=2.5.1  # runtime dep — agentic.consensus.validator + verification.pipeline import it at boot

# Development / visualization (not needed for production)
# matplotlib>=3.7
# pytest>=7.0
# streamlit>=1.30
# plotly>=5.18
